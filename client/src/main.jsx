import React from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";          // your global styles (exists in your tree)
import "./styles/Dashboard.css"; // optional: load dashboard styles globally
import "./styles/Login.css";     // optional: load login styles globally

const container = document.getElementById("root");
const root = createRoot(container);

root.render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
